import React from "react";
import { ComboboxItemProps } from "./Combobox.types";

const ComboboxItem: React.FC<ComboboxItemProps> = ({ children }) => {
  // This is just a marker component, actual rendering happens in Combobox
  return null;
};

export default ComboboxItem;
